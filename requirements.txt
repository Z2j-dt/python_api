# 数据平台统一门户 + 各子服务 - 根目录虚拟环境用
# 在项目根目录执行: python -m venv venv && venv\Scripts\activate (Win) / source venv/bin/activate (Linux) 后 pip install -r requirements.txt
# Python 3.7 兼容：MarkupSafe 2.0.x；Werkzeug<2.2 与之兼容；uvicorn 不用 [standard] 避免 watchfiles/maturin 构建

# 门户 + ASGI
MarkupSafe>=2.0,<2.1
flask>=2.0.0,<2.2
requests>=2.28.0
werkzeug>=2.0.0,<2.2
fastapi>=0.100.0
uvicorn>=0.22.0

# 数据字典 hive_metadata
PyMySQL>=1.0.3
Flask-Caching>=2.0.2

# 数据血缘 sql_lineage_web（8.0.x 与 8.1.x 均可）
mysql-connector-python>=8.0.0

# 业务场景 sr_api（与 portal 的 fastapi/uvicorn 版本兼容即可）
python-dotenv>=0.21.0
pydantic>=1.10.0,<2.0

# Excel 入库 Hive excel_to_hive（Impala 执行用 impala-shell，无需 impyla）
pandas>=1.3.0
openpyxl>=3.0.0
xlrd>=2.0.0
pypinyin>=0.49.0
# impyla 可选：安装时可能需 CFLAGS="-std=c99"；不装则 excel_to_hive 自动用 impala-shell 执行 SQL
# impyla>=0.18.0
